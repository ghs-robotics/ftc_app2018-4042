package org.firstinspires.ftc.teamcode.control.teascript;

public class ExampleSub extends Subsystem{

    // Variables which will be set by outside sources
    private boolean example;

    public ExampleSub() {
        example = false;
    }

    /**
     * TODO: Write this function
     *
     * Should move actuators depending on the variables set, such as example
     */
    @Override
    public void update() {
        if (example) {
            //code
        }
    }
}
